# CLAUDE.md — Evolution Z

Этот файл читается Claude Code автоматически при каждом запуске в папке проекта.
Следуй этим правилам при любой работе с кодом.

---

## Проект

**Название:** Evolution Z (Проект ЭДЕМ)
**Жанр:** Top-down survival action
**Стек:** Python 3.12.1 · pygame-ce · dataclasses · json (`pytmx` — зарезервирован для будущих TMX-карт, в v1 не используется)
**Цель:** Экзаменационная работа по ООП — архитектура важнее количества контента

---

## Структура проекта

```
project_eden/
├── main.py            # точка входа, Game loop, GameStateManager
├── settings.py        # все константы — FPS, размеры, цвета, пути
├── core/              # базовые классы: GameObject, Entity, Weapon, Item
├── entities/          # конкретные объекты: Player, Zombie, Boss, Bullet
│   └── weapons/       # Pistol, Rifle, Shotgun
├── systems/           # игровые системы: combat, inventory, quest, save...
├── ui/                # экраны: MainMenu, GameScreen, PauseMenu, InventoryUI...
├── data/              # dataclass-структуры: PlayerData, SaveFile...
├── assets/
│   ├── data/          # JSON-конфиги: weapons.json, enemies.json, quests.json
│   ├── maps/          # (зарезервировано) Tiled .tmx — в v1 не используются
│   ├── sprites/       # (зарезервировано) PNG спрайты — в v1 рендер примитивами
│   └── sounds/        # (зарезервировано) SFX и музыка — в v1 не реализованы
└── saves/             # единый файл быстрого сохранения (генерируется в рантайме)
```

> **Статус v1 (фактический):** один уровень — сетка стен задаётся в коде
> (`systems/game_world.py`), а не из TMX. Мульти-главы, аудио и спрайты — вне скоупа v1.

---

## Принципы ООП — обязательно демонстрировать

Проект — экзаменационная работа. Каждый принцип должен быть виден в коде явно.

### Наследование
Иерархия идёт строго сверху вниз:
```
GameObject → Entity → Player
                    → Zombie → WalkerZombie
                             → RunnerZombie
                             → SpitterZombie
                    → Boss   → PatientZeroBoss
```
- Новые типы врагов — всегда через наследование от `Zombie`, не копипаст
- Никогда не наследоваться от двух классов сущностей одновременно

### Инкапсуляция
- Поля здоровья, опыта, голода — **всегда приватные** (`_health`, `_xp`)
- Доступ только через `@property` и методы (`take_damage()`, `heal()`)
- Изменение приватного поля напрямую извне — ошибка архитектуры
```python
# правильно
player.take_damage(10)
print(player.health)

# неправильно
player._health -= 10
```

### Полиморфизм
- Все враги обновляются через `enemy.update(dt)` — единый вызов, разное поведение
- Метод `attack()` переопределяется в каждом подклассе
- Не использовать `isinstance()` для выбора поведения — это нарушение полиморфизма

### Композиция
- `Player` содержит системы как поля: `self.inventory`, `self.health`, `self.hunger`
- Системы не наследуются — они подключаются
- Если логика не связана напрямую с сущностью — выноси в отдельную систему

---

## Архитектурные правила

### EventBus — главный способ связи систем
```python
# эмитить событие
EventBus.emit("entity_damaged", {"target": self, "amount": 10})

# подписаться
EventBus.on("entity_damaged", self._on_damage)
```
- **Запрещено** импортировать `HUD` в `Player` или `CombatSystem` в `Quest`
- Все побочные эффекты (звук, UI, квесты) — через события, не прямые вызовы

### Данные — через JSON, не хардкод
- Статы врагов, урон оружия, HP босса — в `assets/data/*.json`
- В коде — только загрузка и использование, никаких магических чисел
```python
# правильно
self.damage = config["damage"]

# неправильно
self.damage = 25
```

### Константы — только в settings.py
```python
# правильно
from settings import FPS, SCREEN_W, SCREEN_H

# неправильно
clock.tick(60)
screen = pygame.display.set_mode((1280, 720))
```

### Экраны — через стек состояний
```python
state_manager.push(InventoryUI())   # открыть
state_manager.pop()                  # закрыть, вернуться к предыдущему
```
- Не использовать булевы флаги `is_paused`, `show_inventory`

---

## Стиль кода

- **Язык кода:** английский (имена классов, методов, переменных, комментарии в коде)
- **Язык документации:** русский (docstring, CLAUDE.md, README)
- Type hints обязательны для всех публичных методов
- Docstring на каждом классе — минимум одна строка
- Максимальная длина строки: 100 символов
- Именование: `snake_case` для методов и переменных, `PascalCase` для классов

```python
class WalkerZombie(Zombie):
    """Медленный зомби с высоким HP. Патрулирует и преследует при обнаружении."""

    def update_ai(self, dt: float) -> None:
        ...

    def attack(self, target: Entity) -> None:
        ...
```

---

## Порядок разработки (итерации)

При добавлении нового функционала соблюдай порядок слоёв:

```
1. data/       → определи структуру данных (@dataclass)
2. core/       → расширь базовый класс если нужно
3. entities/   → реализуй конкретный объект
4. systems/    → добавь логику системы
5. ui/         → отобрази в интерфейсе
```

Никогда не начинай с UI — UI всегда последний.

---

## Запрещено в этом проекте

- `import pygame` вне `main.py`, `settings.py` и классов, которые явно работают с рендером
- Прямые вызовы между системами в обход EventBus
- Глобальные переменные вне `settings.py`
- Логика геймплея внутри UI-классов
- `print()` для отладки в продакшн-коде — использовать `logging`
- Процедурная генерация карт (вне скоупа v1)
- Крафт, строительство базы, мультиплеер (вне скоупа v1)

---

## Чеклист после каждой фичи

После реализации любого функционала — строго в таком порядке:

```bash
# 1. Линтинг
ruff check .

# 2. Проверка типов
mypy . --ignore-missing-imports

# 3. Тесты
pytest tests/

# 4. Ручной запуск игры
python main.py
```

Не считать фичу завершённой, пока все 4 шага не пройдены. Если что-то падает — фиксировать до отчёта.

---

## Команды

```bash
# запуск игры
python main.py

# установка зависимостей
pip install -r requirements.txt

# проверка типов (если установлен mypy)
mypy . --ignore-missing-imports

# тесты (если установлен pytest)
pytest tests/
```

---

## Локации (проектный замысел — НЕ реализовано в v1)

> Ниже — нарративный замысел четырёх глав. **В v1 не реализовано:** TMX-карт нет,
> игра проходит на одном уровне (стены — в `systems/game_world.py`), а босс и компоненты
> вакцины размещены на нём же. Таблица оставлена как ориентир для будущих спринтов.

| Глава | Локация (замысел) | Ключевое событие |
|---|---|---|
| 1 | Бункер А-1 | узнать о вирусе, компонент №1 |
| 2 | Бункер Б-3 | восстановить питание, компонент №2 |
| 3 | Лаборатория ЭДЕМ-5 | мутанты, компонент №3 |
| Финал | ЭДЕМ-7 | PatientZeroBoss, финал |

---

*Обновляй этот файл при изменении архитектурных решений.*