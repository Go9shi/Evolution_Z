# Evolution Z — зависимости
# Python 3.12.1
#
# Установка:
#   pip install -r requirements.txt

# рендер, ввод, звук (форк pygame с активной поддержкой)
pygame-ce==2.5.7

# (зарезервировано) загрузка карт Tiled (.tmx) — в v1 не используется,
# оставлено для будущей работы с картами
pytmx==3.32

# dev: QA-инструменты
ruff
mypy
pytest